// Empty module stub: used by metro.config.js to replace platform-incompatible
// packages (e.g. react-dom on native, react-native-view-shot on web) so the
// bundler can tree-shake dead imports.
export default undefined;
